# Explain This — extension folder

This is the folder to pick when Chrome asks you to **Load unpacked**.

Install steps, requirements, and everything else live in the [main README](../README.md).

After changing anything here, reload the extension from `chrome://extensions` and reload the page you're testing on. Chrome won't run the extension on its own `chrome://` pages.
